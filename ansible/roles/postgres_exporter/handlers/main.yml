---
- name: Restart postgres_exporter
  ansible.builtin.systemd:
    name: postgres_exporter
    state: restarted

- name: Reload systemd and restart postgres_exporter
  ansible.builtin.systemd:
    daemon_reload: true
    name: postgres_exporter
    state: restarted
