---
- name: Restart prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restarted

- name: Reload systemd and restart prometheus
  ansible.builtin.systemd:
    daemon_reload: true
    name: prometheus
    state: restarted
