---
- name: Install pip
  apt:
    name: python3-pip
    state: present
    update_cache: true

- name: Install Patroni and its etcd3/psycopg2 dependencies
  pip:
    name:
      - patroni[etcd3]==4.1.5
      - psycopg2-binary
    executable: pip3
    extra_args: --break-system-packages