---
- name: Allow Patroni REST API traffic through ufw (VPC-internal only — HAProxy health checks need this cross-node, unlike our previous local-only testing)
  ufw:
    rule: allow
    port: "8008"
    proto: tcp
    src: 10.0.0.0/16