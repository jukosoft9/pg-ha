---
- name: Allow etcd client/peer traffic through ufw (VPC-internal only — same reasoning as the PostgreSQL ufw rule from Stage 6)
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
    src: 10.0.0.0/16
  loop:
    - "2379"
    - "2380"
